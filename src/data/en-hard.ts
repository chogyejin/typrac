export const enHard: string[] = [
  'The Byzantine fault-tolerant consensus algorithm requires 3f+1 nodes to tolerate f faulty nodes, ensuring system reliability even under adversarial conditions.',
  'Asynchronous programming in JavaScript leverages the event loop, Promises, and async/await syntax to handle I/O-bound operations without blocking the main thread.',
  'The time complexity of quicksort is O(n log n) on average, but degrades to O(n^2) in the worst case when the pivot selection is consistently poor.',
  'Kubernetes orchestrates containerized workloads across a cluster, providing auto-scaling, self-healing, and rolling updates with zero downtime deployments.',
  'The CAP theorem states that a distributed system can only guarantee two of the following three properties: Consistency, Availability, and Partition tolerance.',
  'GraphQL\'s type system enables clients to request exactly the data they need, reducing over-fetching and under-fetching compared to traditional REST APIs.',
  'Cryptographic hash functions like SHA-256 produce a fixed-size digest; even a 1-bit change in input causes ~50% of output bits to flip (avalanche effect).',
  'TypeScript\'s structural type system uses duck typing: if an object has the required shape (properties + methods), it satisfies the interface regardless of declaration.',
  'Rust\'s ownership model enforces memory safety at compile time: each value has exactly one owner, and the borrow checker prevents data races at zero runtime cost.',
  'The Fibonacci sequence (0, 1, 1, 2, 3, 5, 8, 13, 21...) can be computed in O(log n) time using matrix exponentiation or Binet\'s closed-form formula.',
];
